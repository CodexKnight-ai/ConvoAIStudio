syntax = "proto3";

service AIEngine {
    rpc StartPodcast(stream PodcastRequest)
        returns (stream PodcastResponse);
}

message PodcastRequest {
    string podcast_id = 1;
    string title = 2;
}
message PodcastResponse {
    uint32 sequence = 1;
    string transcript = 2;
}